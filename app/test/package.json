{
  "name": "test",
  "version": "1.0.0",
  "description": "The SPaT API will return the SPaT stream from the Yunex Traffic m60. Every API call will return the SPaT message at a particular point in time.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
